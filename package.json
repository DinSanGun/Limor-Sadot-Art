{
  "name": "limor-sadot",
  "version": "0.1.0",
  "dependencies": {
    "@emailjs/browser": "^3.4.0",
    "dotenv": "^10.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.2.1",
    "react-scripts": "5.0.0"
  },
  "scripts": {
    "watch:sass": "sass --watch src/main.scss:src/index.css",
    "server": "npm-run-all --parallel start watch:sass",
    "compile:sass": "sass src/main.scss public/css/style.comp.css",
    "prefix:css": "postcss --use autoprefixer -b \"last 10 versions\" public/css/style.comp.css -o public/css/style.prefix.css",
    "compress:css": "sass public/css/style.prefix.css public/css/style.css --style=compressed",
    "build:css": "npm-run-all compile:sass prefix:css compress:css",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^9.1.0",
    "sass": "^1.70.0"
  }
}